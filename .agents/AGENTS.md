# Project Rules

## Git Commit Protocol
- Luôn thực hiện Git commit trước khi chạy lệnh build hoặc deploy hosting (e.g. `firebase deploy`).
- Thông điệp commit (commit message) cần ghi rõ ngắn gọn nội dung các thay đổi vừa thực hiện.
